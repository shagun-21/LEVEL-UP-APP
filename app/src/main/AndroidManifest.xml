<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_level_up">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_level_up"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_Level_Up">
        <activity android:name=".registor"></activity>
        <activity android:name=".menu_item5" />
        <activity android:name=".menu_item4" />
        <activity android:name=".menu_item3" />
        <activity android:name=".menu_item2" />
        <activity android:name=".menu_item1" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreenBaackground">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>